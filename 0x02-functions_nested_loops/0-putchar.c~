#include <stdio.h>
/**
 * main - Entry point
 * Description: 0. _putchar task
 * Return: Always zero(0)
 */
int main(void)
{
	const int size = 8;
	int ch[size] = {95, 112, 117, 116, 99, 104, 97, 114};

	for (int i = 0; i < size; i++)
		putchar(ch[i]);
	putchar('\n');
	return (0);
}
