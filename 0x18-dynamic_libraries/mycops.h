#ifndef MYCOPS_H
#define MYCOPS_H

#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

int add(int, int);
int sub(int, int);
int mul(int, int);
int div(int, int);
int mod(int, int);


#endif /* MYCOPS_H */
